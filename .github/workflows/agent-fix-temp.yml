name: Temporary Agent gap fix

on:
  push:
    branches:
      - agent-api-foundation

permissions:
  contents: write

concurrency:
  group: temporary-agent-gap-fix
  cancel-in-progress: true

jobs:
  fix-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent-api-foundation
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply audited fixes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def read(path):
              return Path(path).read_text(encoding="utf-8")

          def write(path, content):
              Path(path).write_text(content, encoding="utf-8", newline="\n")

          def replace_once(path, old, new):
              text = read(path)
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f"{path}: expected one occurrence, found {count}: {old[:80]!r}")
              write(path, text.replace(old, new, 1))

          def regex_once(path, pattern, replacement):
              text = read(path)
              changed, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
              if count != 1:
                  raise RuntimeError(f"{path}: regex expected one occurrence, found {count}: {pattern}")
              write(path, changed)

          # One canonical protocol parser now returns both semantic commands and display-safe visible content.
          write("src/Mezhs.Agent.Api/Commands/Parser.cs", r'''namespace Mezhs.Agent.Commands;

public sealed record Command(string Name, string? Body);

public sealed record CommandBatch(
    IReadOnlyList<Command> Commands,
    string VisibleContent);

public sealed class CommandParseException(string message) : Exception(message);

public sealed class Parser
{
    public CommandBatch Parse(string content)
    {
        var lines = ReadLines(content);
        var commands = new List<Command>();
        var visible = new List<string>();

        for (var i = 0; i < lines.Count; i++)
        {
            var text = lines[i].Text.Trim();
            if (!TryTag(text, out var name, out var closing))
            {
                visible.Add(lines[i].Text);
                continue;
            }
            if (closing)
                throw new CommandParseException($"Unexpected closing command </{name}>.");

            var closeIndex = FindClosingTag(lines, i + 1, name);
            if (closeIndex < 0)
            {
                commands.Add(new Command(name, null));
                continue;
            }

            var bodyStart = lines[i].End;
            var bodyEnd = closeIndex == i + 1
                ? bodyStart
                : lines[closeIndex - 1].ContentEnd;
            commands.Add(new Command(name, content.Substring(bodyStart, bodyEnd - bodyStart)));
            i = closeIndex;
        }

        return new CommandBatch(
            commands,
            string.Join('\n', visible).Trim());
    }

    private static int FindClosingTag(IReadOnlyList<LineSlice> lines, int start, string name)
    {
        for (var i = start; i < lines.Count; i++)
        {
            var text = lines[i].Text.Trim();
            if (!TryTag(text, out var candidate, out var closing))
                continue;
            if (closing && string.Equals(candidate, name, StringComparison.Ordinal))
                return i;
            return -1;
        }
        return -1;
    }

    private static bool TryTag(string text, out string name, out bool closing)
    {
        name = string.Empty;
        closing = false;
        if (text.Length < 3 || text[0] != '<' || text[^1] != '>')
            return false;

        var value = text.AsSpan(1, text.Length - 2);
        if (value.Length > 0 && value[0] == '/')
        {
            closing = true;
            value = value[1..];
        }
        if (!TryCommandName(value, out name))
            throw new CommandParseException($"Malformed command tag '{text}'.");
        return true;
    }

    private static bool TryCommandName(ReadOnlySpan<char> value, out string name)
    {
        name = string.Empty;
        if (value.Length == 0 || value[0] is < 'A' or > 'Z')
            return false;
        foreach (var character in value)
        {
            if (character is >= 'A' and <= 'Z' or >= '0' and <= '9' or '_' or '-')
                continue;
            return false;
        }
        name = value.ToString();
        return true;
    }

    private static IReadOnlyList<LineSlice> ReadLines(string content)
    {
        var result = new List<LineSlice>();
        var position = 0;
        while (position < content.Length)
        {
            var start = position;
            while (position < content.Length && content[position] is not ('\r' or '\n'))
                position++;
            var contentEnd = position;
            if (position < content.Length && content[position] == '\r')
                position++;
            if (position < content.Length && content[position] == '\n')
                position++;
            result.Add(new LineSlice(
                content.Substring(start, contentEnd - start),
                contentEnd,
                position));
        }
        return result;
    }

    private sealed record LineSlice(string Text, int ContentEnd, int End);
}
''')

          # Agent API message DTOs carry canonical protocol interpretation to Agent Web.
          models = read("src/Mezhs.Agent.Api/Models/AgentModels.cs")
          if not models.startswith("using Mezhs.Api.Contracts;"):
              models = "using Mezhs.Api.Contracts;\n\n" + models
          marker = '''public sealed record AgentChatView(
    string ChatId,
    string PolicyId,
    string OriginSource,
    string? OriginReference,
    bool Paused,
    string? Title,
    string? ConnectionId,
    DateTimeOffset CreatedAt,
    DateTimeOffset UpdatedAt);
'''
          insertion = marker + '''
public sealed record AgentProtocolCommandView(
    string Name,
    string? Body,
    int? CommandIndex);

public sealed record AgentChatMessageView(
    string MessageId,
    string ChatId,
    string ConnectionId,
    string Role,
    string Origin,
    string Content,
    string DisplayContent,
    IReadOnlyList<string> FileIds,
    string? ParentMessageId,
    string? ReplayOfMessageId,
    string? ReplyMessageId,
    MessageStatus Status,
    string? Error,
    DateTimeOffset CreatedAt,
    DateTimeOffset? StartedAt,
    DateTimeOffset? CompletedAt,
    IReadOnlyList<AgentProtocolCommandView> Commands,
    bool CompletionClaimed);
'''
          if models.count(marker) != 1:
              raise RuntimeError("AgentModels.cs: AgentChatView insertion point changed")
          models = models.replace(marker, insertion, 1)
          write("src/Mezhs.Agent.Api/Models/AgentModels.cs", models)

          write("src/Mezhs.Agent.Api/Models/AgentApiMapper.cs", r'''using Mezhs.Agent.Commands;
using Mezhs.Api.Contracts;

namespace Mezhs.Agent.Models;

public static class AgentApiMapper
{
    public static AgentExecutionView ToView(ExecutionRecord record) => new(
        record.ExecutionId,
        record.ParentExecutionId,
        record.CorrelationId,
        record.Kind,
        record.CommandName,
        record.TriggerMessageId,
        record.CommandIndex,
        record.ChatId,
        record.PolicyId,
        record.ConnectionId,
        record.Source,
        record.SourceReference,
        record.Status,
        record.Request,
        record.Result,
        record.Error,
        record.ExitCode,
        record.PolicySnapshot,
        record.CreatedAt,
        record.StartedAt,
        record.CompletedAt);

    public static AgentChatMessageView ToView(
        ApiChatHistoryMessage message,
        Parser parser)
    {
        var displayContent = message.Content;
        var protocolCommands = new List<AgentProtocolCommandView>();
        var completionClaimed = false;

        if (string.Equals(message.Role, "assistant", StringComparison.OrdinalIgnoreCase))
        {
            try
            {
                var batch = parser.Parse(message.Content);
                displayContent = batch.VisibleContent;
                var executableIndex = 0;
                foreach (var command in batch.Commands)
                {
                    if (Registry.TryGet(command.Name, out var definition) &&
                        definition.Behavior == CommandBehavior.Complete)
                    {
                        completionClaimed = true;
                        continue;
                    }

                    int? commandIndex = null;
                    if (Registry.TryGet(command.Name, out definition))
                        commandIndex = executableIndex++;
                    protocolCommands.Add(new AgentProtocolCommandView(
                        command.Name,
                        command.Body,
                        commandIndex));
                }
            }
            catch (CommandParseException)
            {
                // Stored malformed replies remain readable. Runtime command validation owns the error path.
            }
        }

        return new AgentChatMessageView(
            message.MessageId,
            message.ChatId,
            message.ConnectionId,
            message.Role,
            message.Origin,
            message.Content,
            displayContent,
            message.FileIds,
            message.ParentMessageId,
            message.ReplayOfMessageId,
            message.ReplyMessageId,
            message.Status,
            message.Error,
            message.CreatedAt,
            message.StartedAt,
            message.CompletedAt,
            protocolCommands,
            completionClaimed);
    }
}
''')

          replace_once(
              "src/Mezhs.Agent.Api/Program.cs",
              '''app.MapGet("/v1/agent-chats/{chatId}/messages", async (
    string chatId,
    AgentStore agentStore,
    MezhsApiClient mezhs,
    CancellationToken cancellationToken) =>
{
    if (agentStore.GetAgentChat(chatId) is null)
        return Results.NotFound(new { error = $"Agent chat '{chatId}' was not found." });
    return Results.Ok(await mezhs.GetMessagesAsync(chatId, cancellationToken));
});''',
              '''app.MapGet("/v1/agent-chats/{chatId}/messages", async (
    string chatId,
    AgentStore agentStore,
    MezhsApiClient mezhs,
    Parser parser,
    CancellationToken cancellationToken) =>
{
    if (agentStore.GetAgentChat(chatId) is null)
        return Results.NotFound(new { error = $"Agent chat '{chatId}' was not found." });
    var messages = await mezhs.GetMessagesAsync(chatId, cancellationToken);
    return Results.Ok(messages.Select(message => AgentApiMapper.ToView(message, parser)));
});''')

          # Agent Web consumes protocol interpretation instead of reimplementing the grammar.
          app = read("src/Mezhs.Agent.Web/src/App.tsx")
          app = app.replace(
              '''  content: string;\n  fileIds: string[];''',
              '''  content: string;\n  displayContent: string;\n  commands: ProtocolCommand[];\n  completionClaimed: boolean;\n  fileIds: string[];''',
              1)
          app = app.replace(
              '''type ProtocolCommand = {\n  name: string;\n  body?: string;\n};''',
              '''type ProtocolCommand = {\n  name: string;\n  body?: string;\n  commandIndex?: number;\n};''',
              1)
          app = app.replace(
              '''type ProtocolView = {\n  content: string;\n  commands: ProtocolCommand[];\n  completionClaimed: boolean;\n};''',
              '''type ProtocolView = {\n  commands: ProtocolCommand[];\n  completionClaimed: boolean;\n};''',
              1)
          app = app.replace('const commandName = /^[A-Z][A-Z0-9_-]*$/;\n', '', 1)
          app, count = re.subn(
              r'\nfunction inspectProtocol\(content: string\): ProtocolView \{.*?\n\}\n(?=\nfunction executionFromEnvelope)',
              '',
              app,
              count=1,
              flags=re.S)
          if count != 1:
              raise RuntimeError("App.tsx: inspectProtocol function not found exactly once")
          app = app.replace(
              '''  if (message.role === "assistant") {\n    content = inspectProtocol(message.content).content;''',
              '''  if (message.role === "assistant") {\n    content = message.displayContent;''',
              1)
          app = app.replace(
              '''  for (const message of messages.filter((candidate) => candidate.role === "assistant")) {\n    const protocol = inspectProtocol(message.content);\n    const states = protocol.commands.map((command, index) => ({\n      ...command,\n      execution: executions.find((candidate) =>\n        candidate.triggerMessageId === message.messageId &&\n        candidate.commandIndex === index &&\n        candidate.commandName?.toLocaleUpperCase() === command.name.toLocaleUpperCase()),\n    }));''',
              '''  for (const message of messages.filter((candidate) => candidate.role === "assistant")) {\n    const states = message.commands.map((command) => ({\n      ...command,\n      execution: executions.find((candidate) =>\n        candidate.triggerMessageId === message.messageId &&\n        command.commandIndex !== undefined &&\n        candidate.commandIndex === command.commandIndex &&\n        candidate.commandName?.toLocaleUpperCase() === command.name.toLocaleUpperCase()),\n    }));''',
              1)
          app = app.replace(
              '''  const protocolByMessageId = useMemo(() => new Map(\n    messages\n      .filter((message) => message.role === "assistant")\n      .map((message) => [message.messageId, inspectProtocol(message.content)]),\n  ), [messages]);''',
              '''  const protocolByMessageId = useMemo(() => new Map<string, ProtocolView>(\n    messages\n      .filter((message) => message.role === "assistant")\n      .map((message) => [message.messageId, {\n        commands: message.commands,\n        completionClaimed: message.completionClaimed,\n      }]),\n  ), [messages]);''',
              1)
          if "inspectProtocol" in app or "commandName = /^[A-Z]" in app:
              raise RuntimeError("App.tsx still contains the duplicate protocol parser")
          write("src/Mezhs.Agent.Web/src/App.tsx", app)

          # Completion wording is generated from the selected policy; global runtime text is neutral.
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentPromptBuilder.cs",
              '''    public AgentPrompt BuildContinue() =>\n        new(options.Messages.Continue!, "agent-runtime");''',
              '''    public AgentPrompt BuildContinue(PolicyContext policy) =>\n        new(WithCompletionGuidance(options.Messages.Continue!, policy), "agent-runtime");''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentPromptBuilder.cs",
              '''    public AgentPrompt BuildCommandResults(IReadOnlyList<Result> results)''',
              '''    public AgentPrompt BuildCommandResults(\n        IReadOnlyList<Result> results,\n        PolicyContext policy)''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentPromptBuilder.cs",
              '''        return new AgentPrompt(\n            Format(options.Messages.CommandResults!, "json", json),\n            "command-result");''',
              '''        return new AgentPrompt(\n            WithCompletionGuidance(\n                Format(options.Messages.CommandResults!, "json", json),\n                policy),\n            "command-result");''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentPromptBuilder.cs",
              '''    private static string Truncate(string value) =>''',
              '''    private static string WithCompletionGuidance(\n        string message,\n        PolicyContext policy)\n    {\n        if (!policy.Settings.Completion.RequireDone)\n            return message.TrimEnd();\n\n        var done = Registry.Get(CommandBehavior.Complete);\n        return $"{message.TrimEnd()}\\nWhen the task is complete, return <{done.Name}> on a line by itself.";\n    }\n\n    private static string Truncate(string value) =>''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentWorker.cs",
              '''                    nextPrompt = _prompts.BuildCommandResults(interpretation.Results);''',
              '''                    nextPrompt = _prompts.BuildCommandResults(interpretation.Results, policy);''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentWorker.cs",
              '''                    : _prompts.BuildContinue();''',
              '''                    : _prompts.BuildContinue(policy);''')

          for yaml in ["agent.yaml", "tests/mezhs.agent.test.yaml"]:
              text = read(yaml)
              text = text.replace('    When the task is complete, return <DONE> on a line by itself.\n', '')
              text = text.replace('    Continue the assigned task according to the applicable policy. When the task is complete, return <DONE> on a line by itself.\n', '    Continue the assigned task according to the applicable policy.\n')
              write(yaml, text)

          # Cancellation is root-owned; reconcile a cancellation persisted in the claim/register race window.
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentWorker.cs",
              '''    public ExecutionRecord Cancel(string executionId)\n    {\n        var (record, changed) = _store.RequestCancel(executionId);''',
              '''    public ExecutionRecord Cancel(string executionId)\n    {\n        var current = _store.GetExecution(executionId)\n            ?? throw new ResourceNotFoundException($"Execution '{executionId}' was not found.");\n        if (current.Kind != AgentExecutionKind.Agent || current.ParentExecutionId is not null)\n            throw new RequestValidationException("Only root Agent executions can be cancelled directly.");\n\n        var (record, changed) = _store.RequestCancel(executionId);''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentWorker.cs",
              '''        _cancellations[executionId] = cancellation;\n\n        try''',
              '''        _cancellations[executionId] = cancellation;\n        if (_store.GetExecution(executionId)?.Status == AgentExecutionStatus.CancelRequested)\n            cancellation.Cancel();\n\n        try''')
          replace_once(
              "src/Mezhs.Agent.Api/Services/AgentWorker.cs",
              '''        catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested)''',
              '''        catch (ShellTerminationException ex)\n        {\n            _store.Fail(executionId, ex.Message);\n        }\n        catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested)''')

          replace_once(
              "src/Mezhs.Agent.Api/Persistence/AgentStore.cs",
              '''    public bool Fail(string executionId, string error) =>\n        Finish(\n            executionId,\n            AgentExecutionStatus.Failed,\n            result: null,\n            error,\n            AgentExecutionStatus.Queued,\n            AgentExecutionStatus.Running);''',
              '''    public bool Fail(string executionId, string error) =>\n        Finish(\n            executionId,\n            AgentExecutionStatus.Failed,\n            result: null,\n            error,\n            AgentExecutionStatus.Queued,\n            AgentExecutionStatus.Running,\n            AgentExecutionStatus.CancelRequested);''')
          replace_once(
              "src/Mezhs.Agent.Api/Persistence/AgentStore.cs",
              '''    public bool Fail(string executionId, string error) =>''',
              '''    public bool FailShell(string executionId, string error, string? result) =>\n        Finish(\n            executionId,\n            AgentExecutionStatus.Failed,\n            result,\n            error,\n            AgentExecutionStatus.Running,\n            AgentExecutionStatus.CancelRequested);\n\n    public bool Fail(string executionId, string error) =>''')

          # Shell timeout/cancellation never silently claims a process was terminated; partial output is durable.
          write("src/Mezhs.Agent.Api/Commands/Shell.cs", r'''using System.ComponentModel;
using System.Diagnostics;
using System.Text;
using Mezhs.Agent.Configuration;
using Mezhs.Agent.Models;
using Mezhs.Agent.Persistence;

namespace Mezhs.Agent.Commands;

public sealed class ShellTerminationException(string message) : Exception(message);

public sealed class Shell(
    AgentStore store,
    AgentOptions options)
{
    public async Task<Result> ExecuteAsync(
        ExecutionContext context,
        string commandText,
        CancellationToken cancellationToken)
    {
        var definition = Registry.Get(CommandBehavior.Shell);
        if (string.IsNullOrWhiteSpace(commandText))
            return new Result(definition.Name, null, false, null, null, "Shell command body cannot be empty.");

        var child = store.CreateChildExecution(
            context.ParentExecution,
            AgentExecutionKind.Shell,
            definition.Name,
            commandText,
            context.TriggerMessageId,
            context.CommandIndex);
        if (!store.TryMarkRunning(child.ExecutionId))
            return new Result(definition.Name, child.ExecutionId, false, null, null, "Shell execution could not enter the running state.");

        var invocation = CreateInvocation(context.ParentExecution, child, commandText);
        using var process = new Process { StartInfo = invocation.StartInfo };
        using var commandCancellation = CancellationTokenSource.CreateLinkedTokenSource(cancellationToken);
        commandCancellation.CancelAfter(context.Timeout);

        Task<string>? stdoutTask = null;
        Task<string>? stderrTask = null;
        try
        {
            if (!process.Start())
                throw new InvalidOperationException("Host shell process could not be started.");

            stdoutTask = process.StandardOutput.ReadToEndAsync();
            stderrTask = process.StandardError.ReadToEndAsync();
            await WriteInputAsync(process, invocation.Payload, commandCancellation.Token);
            await process.WaitForExitAsync(commandCancellation.Token);
            var streams = await Task.WhenAll(stdoutTask, stderrTask);
            var result = FormatResult(streams[0], streams[1]);
            var exitCode = process.ExitCode;

            cancellationToken.ThrowIfCancellationRequested();
            if (!store.CompleteShell(child.ExecutionId, exitCode, result))
            {
                if (store.GetExecution(child.ExecutionId)?.Status == AgentExecutionStatus.CancelRequested)
                {
                    store.CompleteCancellation(child.ExecutionId);
                    throw new OperationCanceledException(cancellationToken);
                }
                throw new InvalidOperationException("Shell execution changed state before its result could be recorded.");
            }

            return new Result(
                definition.Name,
                child.ExecutionId,
                exitCode == 0,
                exitCode,
                result,
                exitCode == 0 ? null : $"Shell exited with code {exitCode}.");
        }
        catch (OperationCanceledException) when (!cancellationToken.IsCancellationRequested)
        {
            var terminationError = await TerminateAsync(process);
            var result = await CaptureAvailableOutputAsync(stdoutTask, stderrTask);
            var seconds = context.Timeout.TotalSeconds.ToString("0.###", System.Globalization.CultureInfo.InvariantCulture);
            var error = $"Shell command timed out after {seconds} seconds.";
            if (terminationError is not null)
                error = $"{error} {terminationError}";
            PersistFailure(child.ExecutionId, error, result);
            if (terminationError is not null)
                throw new ShellTerminationException(error);
            return new Result(definition.Name, child.ExecutionId, false, null, EmptyToNull(result), error);
        }
        catch (OperationCanceledException)
        {
            var terminationError = await TerminateAsync(process);
            var result = await CaptureAvailableOutputAsync(stdoutTask, stderrTask);
            if (terminationError is not null)
            {
                var error = $"Shell cancellation was requested, but termination could not be confirmed. {terminationError}";
                PersistFailure(child.ExecutionId, error, result);
                throw new ShellTerminationException(error);
            }

            store.RequestCancel(child.ExecutionId);
            store.CompleteCancellation(child.ExecutionId);
            throw;
        }
        catch (Exception ex) when (ex is not ShellTerminationException)
        {
            var terminationError = await TerminateAsync(process);
            var result = await CaptureAvailableOutputAsync(stdoutTask, stderrTask);
            var error = terminationError is null ? ex.Message : $"{ex.Message} {terminationError}";
            PersistFailure(child.ExecutionId, error, result);
            if (terminationError is not null)
                throw new ShellTerminationException(error);
            return new Result(definition.Name, child.ExecutionId, false, null, EmptyToNull(result), error);
        }
        finally
        {
            try { process.StandardInput.Close(); } catch (InvalidOperationException) { }
        }
    }

    private void PersistFailure(string executionId, string error, string result)
    {
        if (!store.FailShell(executionId, error, EmptyToNull(result)))
            throw new InvalidOperationException("Shell execution changed state before its failure could be recorded.");
    }

    private ShellInvocation CreateInvocation(
        ExecutionRecord parent,
        ExecutionRecord child,
        string commandText)
    {
        var startInfo = CreateBaseStartInfo(parent, child);
        if (OperatingSystem.IsWindows())
        {
            startInfo.FileName = Environment.GetEnvironmentVariable("ComSpec") ?? "cmd.exe";
            startInfo.ArgumentList.Add("/D");
            startInfo.ArgumentList.Add("/Q");
            return new ShellInvocation(startInfo, "@chcp 65001>nul\r\n" + commandText + "\r\n");
        }

        startInfo.FileName = "/bin/sh";
        return new ShellInvocation(startInfo, commandText + "\n");
    }

    private ProcessStartInfo CreateBaseStartInfo(
        ExecutionRecord parent,
        ExecutionRecord child)
    {
        var startInfo = new ProcessStartInfo
        {
            UseShellExecute = false,
            RedirectStandardInput = true,
            RedirectStandardOutput = true,
            RedirectStandardError = true,
            StandardInputEncoding = Encoding.UTF8,
            CreateNoWindow = true,
            WorkingDirectory = options.Workspace
        };

        foreach (var (name, value) in parent.Environment)
            startInfo.Environment[name] = value;
        startInfo.Environment["MEZHS_EXECUTION_ID"] = child.ExecutionId;
        startInfo.Environment["MEZHS_PARENT_EXECUTION_ID"] = parent.ExecutionId;
        startInfo.Environment["MEZHS_CORRELATION_ID"] = parent.CorrelationId;
        startInfo.Environment["MEZHS_SOURCE"] = parent.Source;
        startInfo.Environment["MEZHS_WORKSPACE"] = options.Workspace;
        if (!string.IsNullOrWhiteSpace(parent.ChatId))
            startInfo.Environment["MEZHS_CHAT_ID"] = parent.ChatId;

        if (OperatingSystem.IsWindows())
        {
            startInfo.StandardOutputEncoding = Encoding.UTF8;
            startInfo.StandardErrorEncoding = Encoding.UTF8;
        }
        return startInfo;
    }

    private static async Task WriteInputAsync(
        Process process,
        string payload,
        CancellationToken cancellationToken)
    {
        await process.StandardInput.WriteAsync(payload.AsMemory(), cancellationToken);
        await process.StandardInput.FlushAsync(cancellationToken);
        process.StandardInput.Close();
    }

    private static async Task<string?> TerminateAsync(Process process)
    {
        if (HasExitedOrNotStarted(process))
            return null;

        try
        {
            process.Kill(entireProcessTree: true);
        }
        catch (Exception ex) when (ex is InvalidOperationException or Win32Exception)
        {
            if (HasExitedOrNotStarted(process))
                return null;
            return $"Shell process-tree termination failed: {ex.Message}";
        }

        try
        {
            await process.WaitForExitAsync().WaitAsync(TimeSpan.FromSeconds(5));
        }
        catch (Exception ex) when (ex is InvalidOperationException or Win32Exception or TimeoutException)
        {
            if (HasExitedOrNotStarted(process))
                return null;
            return ex is TimeoutException
                ? "Shell process-tree termination was not confirmed within 5 seconds."
                : $"Shell process-tree termination could not be confirmed: {ex.Message}";
        }

        return HasExitedOrNotStarted(process)
            ? null
            : "Shell process-tree termination could not be confirmed.";
    }

    private static bool HasExitedOrNotStarted(Process process)
    {
        try { return process.HasExited; }
        catch (InvalidOperationException) { return true; }
    }

    private static async Task<string> CaptureAvailableOutputAsync(
        Task<string>? stdoutTask,
        Task<string>? stderrTask)
    {
        if (stdoutTask is null || stderrTask is null)
            return string.Empty;

        try
        {
            var streams = await Task.WhenAll(stdoutTask, stderrTask).WaitAsync(TimeSpan.FromSeconds(2));
            return FormatResult(streams[0], streams[1]);
        }
        catch (TimeoutException)
        {
            return string.Empty;
        }
    }

    private static string FormatResult(string stdout, string stderr)
    {
        var result = new StringBuilder();
        AppendStream(result, "stdout", stdout);
        AppendStream(result, "stderr", stderr);
        return result.ToString();
    }

    private static void AppendStream(StringBuilder result, string name, string value)
    {
        if (string.IsNullOrEmpty(value))
            return;
        if (result.Length > 0)
            result.AppendLine();

        var normalized = value.TrimEnd('\r', '\n');
        var firstBreak = normalized.IndexOfAny(['\r', '\n']);
        if (firstBreak < 0)
        {
            result.Append(name).Append(": ").Append(normalized);
            return;
        }

        result.Append(name).Append(": ").Append(normalized[..firstBreak]);
        var remainderStart = firstBreak;
        while (remainderStart < normalized.Length && normalized[remainderStart] is '\r' or '\n')
            remainderStart++;
        if (remainderStart < normalized.Length)
            result.AppendLine().Append(normalized[remainderStart..]);
    }

    private static string? EmptyToNull(string value) =>
        string.IsNullOrEmpty(value) ? null : value;

    private sealed record ShellInvocation(ProcessStartInfo StartInfo, string Payload);
}
''')

          # Behavior tests for the exact regressions found in the audit.
          test = read("tests/policy-decoder.ps1")
          test = test.replace(
              '''using Mezhs.Agent.Policy;\nusing Microsoft.Data.Sqlite;''',
              '''using Mezhs.Agent.Policy;\nusing Mezhs.Api.Contracts;\nusing Microsoft.Data.Sqlite;''',
              1)
          test = test.replace(
              '''Assert(parsed.Commands[1].Name == "DONE" && parsed.Commands[1].Body is null,\n    "DONE marker was not parsed as a marker command.");''',
              '''Assert(parsed.Commands[1].Name == "DONE" && parsed.Commands[1].Body is null,\n    "DONE marker was not parsed as a marker command.");\nAssert(parsed.VisibleContent == "before\\nafter",\n    $"Protocol parser did not remove executable protocol from display content: '{parsed.VisibleContent}'.");\n\nvar mappedMessage = AgentApiMapper.ToView(\n    new ApiChatHistoryMessage(\n        "msg-view", "chat-view", normal.ConnectionId, "assistant", "agent",\n        "visible before\\n<SH>\\necho mapped\\n</SH>\\n<DONE>\\nvisible after",\n        [], null, null, null, MessageStatus.Completed, null, now, now, now),\n    parser);\nAssert(mappedMessage.DisplayContent == "visible before\\nvisible after",\n    "Agent API did not own protocol stripping for Web display.");\nAssert(mappedMessage.Commands.Count == 1 && mappedMessage.Commands[0].Name == "SH" &&\n       mappedMessage.Commands[0].Body == "echo mapped" && mappedMessage.Commands[0].CommandIndex == 0 &&\n       mappedMessage.CompletionClaimed,\n    "Agent API protocol view lost SH body/index or DONE claim.");\n\nvar promptBuilder = new AgentPromptBuilder(options);\nAssert(!promptBuilder.BuildContinue(normal).Content.Contains("<DONE>", StringComparison.Ordinal),\n    "requireDone=false continuation still instructs DONE.");\nAssert(promptBuilder.BuildContinue(done).Content.Contains("<DONE>", StringComparison.Ordinal),\n    "requireDone=true continuation lost DONE guidance.");''',
              1)
          test = test.replace(
              '''    var timeoutText = OperatingSystem.IsWindows() ? "ping -n 6 127.0.0.1 >nul" : "sleep 5";''',
              '''    var timeoutText = OperatingSystem.IsWindows()\n        ? "echo BEFORE_TIMEOUT & ping -n 6 127.0.0.1 >nul"\n        : "echo BEFORE_TIMEOUT; sleep 5";''',
              1)
          test = test.replace(
              '''    Assert(timeoutChild.Status == AgentExecutionStatus.Failed, "Timed-out shell was not persisted as failed evidence.");''',
              '''    Assert(timeoutChild.Status == AgentExecutionStatus.Failed, "Timed-out shell was not persisted as failed evidence.");\n    Assert(timeoutChild.Result?.Contains("BEFORE_TIMEOUT", StringComparison.Ordinal) == true,\n        "Partial shell output was returned transiently but not persisted with timeout evidence.");''',
              1)
          write("tests/policy-decoder.ps1", test)

          webtest = read("tests/agent-web-shared-ui.ps1")
          webtest = webtest.replace(
              '''$agentApp = Get-Content (Join-Path $root "src/Mezhs.Agent.Web/src/App.tsx") -Raw''',
              '''$agentApp = Get-Content (Join-Path $root "src/Mezhs.Agent.Web/src/App.tsx") -Raw\n$agentProgram = Get-Content (Join-Path $root "src/Mezhs.Agent.Api/Program.cs") -Raw\n$agentMapper = Get-Content (Join-Path $root "src/Mezhs.Agent.Api/Models/AgentApiMapper.cs") -Raw''',
              1)
          webtest = webtest.replace(
              '''if ($agentApp -notmatch 'candidate\\.triggerMessageId === message\\.messageId' -or\n    $agentApp -notmatch 'candidate\\.commandIndex === index') {\n    throw "Agent command cards reconstruct execution state instead of using durable command identity."\n}''',
              '''if ($agentApp -notmatch 'candidate\\.triggerMessageId === message\\.messageId' -or\n    $agentApp -notmatch 'candidate\\.commandIndex === command\\.commandIndex') {\n    throw "Agent command cards reconstruct execution state instead of using durable command identity."\n}\nif ($agentApp -match 'inspectProtocol|commandName = /\\^\\[A-Z\\]' -or\n    $agentApp -notmatch 'message\\.displayContent' -or\n    $agentApp -notmatch 'message\\.commands' -or\n    $agentMapper -notmatch 'parser\\.Parse\\(message\\.Content\\)' -or\n    $agentProgram -notmatch 'AgentApiMapper\\.ToView\\(message, parser\\)') {\n    throw "Agent Web still owns a duplicate command-protocol parser instead of consuming Agent API semantics."\n}''',
              1)
          webtest = webtest.replace(
              '''Write-Host "PASS: shared Markdown/composer behavior and durable Agent command evidence rendering are wired through their owning components."''',
              '''Write-Host "PASS: shared Markdown/composer behavior and canonical Agent API protocol/evidence rendering are wired through their owning components."''',
              1)
          write("tests/agent-web-shared-ui.ps1", webtest)

          arch = read("tests/agent-architecture.ps1")
          arch = arch.replace(
              '''Assert ($models -match 'CancelRequested' -and $store -match 'RequestCancel' -and $store -match 'CompleteCancellation') "Cancellation acknowledgement state is not represented durably."''',
              '''Assert ($models -match 'CancelRequested' -and $store -match 'RequestCancel' -and $store -match 'CompleteCancellation') "Cancellation acknowledgement state is not represented durably."\nAssert ($worker -match 'Only root Agent executions can be cancelled directly' -and\n    $worker -match '_store\\.GetExecution\\(executionId\\)\\?\\.Status == AgentExecutionStatus\\.CancelRequested') "Cancellation ownership/race reconciliation is missing."\nAssert ($shell -match 'ShellTerminationException' -and $shell -match 'termination could not be confirmed' -and\n    $store -match 'FailShell') "Shell termination uncertainty is still silently converted into normal terminal state."''',
              1)
          write("tests/agent-architecture.ps1", arch)

          resilience = read("tests/agent-runtime-resilience.ps1")
          resilience = resilience.replace(
              '''    $null = Wait-Shell $cancelRunning.chatId @("Running")\n    $cancelResponse = Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:5199/v1/executions/$($cancel.executionId)/cancel"''',
              '''    $runningShell = Wait-Shell $cancelRunning.chatId @("Running")\n\n    $childCancelClient = [Net.Http.HttpClient]::new()\n    try {\n        $childCancelResponse = $childCancelClient.PostAsync(\n            "http://127.0.0.1:5199/v1/executions/$($runningShell.executionId)/cancel",\n            $null).GetAwaiter().GetResult()\n        $childCancelText = $childCancelResponse.Content.ReadAsStringAsync().GetAwaiter().GetResult()\n        try {\n            if ([int]$childCancelResponse.StatusCode -ne 400 -or $childCancelText -notmatch "Only root Agent executions") {\n                throw "Shell child accepted direct cancellation instead of preserving root cancellation ownership. HTTP $([int]$childCancelResponse.StatusCode): $childCancelText"\n            }\n        } finally { $childCancelResponse.Dispose() }\n    } finally { $childCancelClient.Dispose() }\n\n    $cancelResponse = Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:5199/v1/executions/$($cancel.executionId)/cancel"''',
              1)
          write("tests/agent-runtime-resilience.ps1", resilience)

          smoke = read("tests/agent-api-smoke.ps1")
          smoke = smoke.replace(
              '''    $environmentShell = @($environmentExecutions | Where-Object { $_.kind -eq "Shell" })[0]\n    if ($environmentShell.result -notmatch "ENVIRONMENT_OK") { throw "Policy-approved environment variable did not reach the child shell." }''',
              '''    $environmentShell = @($environmentExecutions | Where-Object { $_.kind -eq "Shell" })[0]\n    if ($environmentShell.result -notmatch "ENVIRONMENT_OK") { throw "Policy-approved environment variable did not reach the child shell." }\n\n    $environmentMessages = @(Invoke-RestMethod -Uri "http://127.0.0.1:5199/v1/agent-chats/$($environmentRoot.chatId)/messages")\n    $protocolMessage = @($environmentMessages | Where-Object { $_.role -eq "assistant" -and @($_.commands).Count -gt 0 })[0]\n    if ($null -eq $protocolMessage -or $protocolMessage.commands[0].name -ne "SH" -or\n        $protocolMessage.commands[0].body -notmatch "TEST_AGENT_VALUE" -or\n        $protocolMessage.displayContent -match '</SH>') {\n        throw "Agent message API did not expose canonical SH protocol metadata/display content."\n    }''',
              1)
          write("tests/agent-api-smoke.ps1", smoke)

          # Keep tracking surfaces consistent with the audited state.
          write("TODO.md", '''# TODO

Active architecture follow-ups for the Agent foundation are tracked in `AGENT_API_TODO.md`:

- dedicated MEŽS supervisor/launcher ownership (#14);
- common SQLite/LogSql storage ownership for main/API/Agent persistence.
''')

          todo = read("AGENT_API_TODO.md")
          todo = todo.replace(
              '''- **What changed:** The continuation text explicitly tells the model how completion works, including `<DONE>`, instead of using the ambiguous hardcoded continuation sentence.\n- **How:** Runtime message text is typed configuration owned by the Agent configuration/runtime message settings rather than being embedded inside worker logic. Policy model instructions also expose completion requirements.\n- **Validation:** `tests/policy-decoder.ps1` verifies that compiled policy instructions expose `<DONE>` and successful-evidence requirements where configured.''',
              '''- **What changed:** Runtime continuation/result text is configurable, while completion-marker guidance is generated from the selected policy instead of duplicated in global prose.\n- **How:** `AgentPromptBuilder` appends `<DONE>` guidance only when that policy has `requireDone: true`; policies with `requireDone: false` receive no contradictory marker instruction. Policy model instructions remain the owner of successful-evidence requirements.\n- **Validation:** `tests/policy-decoder.ps1` verifies continuation differs correctly for `requireDone: true` versus `false`.''',
              1)
          todo = todo.replace(
              '''- **What changed:** Shell requests/results use consistent command/evidence cards and code formatting.\n- **How:** The dashboard renders protocol commands and command-result evidence using persisted execution records. Shell command text is displayed as code/preformatted content rather than being reconstructed as ordinary chat text.\n- **Validation:** `tests/agent-web-shared-ui.ps1` verifies durable command-evidence rendering through the owning shared/dashboard components.''',
              '''- **What changed:** Shell requests/results use consistent command/evidence cards and the Web UI no longer implements its own Agent command grammar.\n- **How:** The canonical Agent `Parser` now returns semantic commands plus visible non-protocol content. Agent API maps that into message DTOs (`commands`, `commandIndex`, `completionClaimed`, `displayContent`), and Agent Web only renders those semantics plus persisted execution evidence.\n- **Validation:** `tests/policy-decoder.ps1` parses a real `<SH>... </SH>` + `<DONE>` reply and verifies display stripping/body/index; `tests/agent-web-shared-ui.ps1` rejects a Web-side protocol parser; API smoke verifies the live message shape.''',
              1)
          todo = todo.replace(
              '''- **What changed:** Shell timeout/cancellation owns process termination and durable terminal state instead of leaving a process/execution indefinitely active.\n- **How:** `Shell` uses a linked cancellation token with the policy timeout, kills the entire process tree on timeout/cancellation, waits for bounded shutdown, captures available output, and only then records the resulting terminal state.\n- **Validation:** `tests/policy-decoder.ps1` exercises a real timed-out shell process; runtime cancellation/recovery behavior is covered by `tests/agent-runtime-resilience.ps1`.''',
              '''- **What changed:** Shell timeout/cancellation owns bounded process-tree termination without silently claiming success when termination cannot be confirmed. Partial output is persisted with failed timeout evidence.\n- **How:** `Shell` kills the process tree and waits for bounded shutdown. If termination cannot be confirmed, it records explicit failed shell evidence and raises `ShellTerminationException`, which fails the root execution instead of reporting a normal timeout/cancellation completion.\n- **Validation:** `tests/policy-decoder.ps1` exercises a real timed-out shell and verifies pre-timeout output remains durable; architecture checks require explicit termination-uncertainty handling.''',
              1)
          todo = todo.replace(
              '''- **What changed:** Running work has a durable cancellation-request acknowledgement before it becomes terminal.\n- **How:** `RequestCancel` transitions a running execution to `CancelRequested`. The worker/shell cancellation token stops execution/processes, then `CompleteCancellation` records terminal `Cancelled` with a completion timestamp. A still-queued execution can be cancelled immediately because no process has started.\n- **Validation:** `tests/agent-runtime-resilience.ps1` verifies `CancelRequested` is observable before final `Cancelled`, including durable completion timestamps for root and shell execution.''',
              '''- **What changed:** Running work has a durable cancellation-request acknowledgement, one root cancellation owner, and reconciliation for the claim-to-token-registration race.\n- **How:** Only root Agent executions may be cancelled through the API. `RequestCancel` transitions a running root to `CancelRequested`; after the worker registers its cancellation token it re-reads durable state and immediately cancels if the request landed in the narrow claim/register window. Child shell cancellation stays internal to root cancellation.\n- **Validation:** `tests/agent-runtime-resilience.ps1` rejects direct shell-child cancellation and verifies the normal `CancelRequested` -> `Cancelled` lifecycle; architecture checks require durable race reconciliation.''',
              1)
          write("AGENT_API_TODO.md", todo)
          PY

      - name: Build solution
        run: dotnet build Mezhs.sln -c Release
      - name: Agent architecture
        shell: pwsh
        run: ./tests/agent-architecture.ps1
      - name: Shared Agent UI
        shell: pwsh
        run: ./tests/agent-web-shared-ui.ps1
      - name: Policy, queue, shell, and protocol behavior
        shell: pwsh
        run: ./tests/policy-decoder.ps1
      - name: Agent API smoke
        shell: pwsh
        run: ./tests/agent-api-smoke.ps1
      - name: Agent runtime resilience
        shell: pwsh
        run: ./tests/agent-runtime-resilience.ps1

      - name: Commit validated fixes
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AGENT_API_TODO.md TODO.md agent.yaml tests src
          if (git diff --cached --quiet) { throw "Fix workflow produced no changes." }
          git commit -m "Fix audited Agent protocol and lifecycle gaps [skip ci]"
          git push origin HEAD:agent-api-foundation
